AI Keiba System 2026 (Pro)
MLOps搭載型・完全自動競馬予測システム
📖 概要 (Overview)
**「回収率100%超」という明確なKPIを達成するために開発された、フルスタックの競馬予測AIシステムです。 単なる着順予測にとどまらず、データ収集・前処理・学習・推論・収支管理・モデル監査までをコード上で完結させるMLOps（Machine Learning Operations）**の思想に基づいて設計されています。

従来の「勝ち/負け」の二値分類ではなく、**ランキング学習（Learning to Rank）**を導入することで、上位入線の期待値を高精度に算出します。

🛠 使用技術 (Tech Stack)
言語: Python 3.x

機械学習: LightGBM (lambdarank, gbdt)

最適化: Optuna (ベイズ最適化によるハイパーパラメータチューニング)

データ収集: Selenium (動的スクレイピング), BeautifulSoup, Requests

データ処理: Pandas, NumPy

評価・可視化: Matplotlib, Tabulate

🚀 主な機能と工夫 (Key Features)
1. ランキング学習の導入 (LambdaRank)
不均衡データ（1頭しか勝たない）への対策として、目的関数に lambdarank を採用。評価指標に NDCG (Normalized Discounted Cumulative Gain) を用い、「1着〜3着の馬をいかに上位にランク付けできるか」に特化した学習を行っています。

2. ハイブリッド・スクレイピング
情報の「網羅性」と「即時性」を両立させるため、2つの手法を使い分けています。

過去データ: Requests を用いて高速に数万レース分を蓄積。

本番予測: Selenium を用いて、JavaScriptで描画されるリアルタイムオッズや変更情報（騎手変更・除外）を確実に取得。

3. モデル監査システム (Audit Model)
AI開発で致命的な「データリーク（未来情報の参照）」や「過学習」を防ぐための監査モジュールを実装。

リーク検知: 特徴量重要度（Feature Importance）を監視し、不自然な変数を自動検出。

ロバスト性検証: 「高配当の上位3回を除外しても利益が出るか」をシミュレーションし、運に依存しないモデルであることを証明。

4. 独自の特微量エンジニアリング (Feature Engineering)
ドメイン知識に基づき、26種類以上の独自特徴量を生成。

タイム指数偏差値: そのレースメンバー内での相対的な速さ。

騎手の勢い指数: 直近20走の勝率トレンド。

クロス特徴量: 「騎手 × コース」の相性データなど。

📊 システムフロー (Architecture)
コード スニペット

graph TD
    A[Web Data Source] -->|Selenium/Requests| B(Data Collection)
    B --> C{Preprocessing Pipeline}
    C -->|Feature Engineering| D[Training Data]
    
    subgraph MLOps Cycle
    D --> E[Optuna Tuning]
    E --> F[LightGBM Model]
    F --> G[Inference]
    G --> H[Audit & Validation]
    H -->|Feedback| D
    end
    
    G --> I[Prediction Result]
    I --> J[HTML Report / Discord Alert]
📈 運用実績 (Performance)
検証期間: 2020年〜2024年（バックテスト）および実運用

主な成果:

単勝回収率: 112% (シミュレーション値)

NDCG@3 スコア: 0.78 (ベースライン比 +0.15)

運用体制: 毎週末のレース確定後に自動で答え合わせを行い、HTMLレポートを生成してモデルを継続的にアップデート。

💻 コードスニペット (Code Highlight)
監査機能（Audit）の一部

Python

# マグレ当たり排除テスト
top3_return_sum = sum(h['return'] for h in hits[:3])
audit_return = total_return - top3_return_sum
audit_recovery = (audit_return / total_bet) * 100

if audit_recovery < 100:
    print("⚠️ 警告: 少数の大穴に依存しています。")
else:
    print("✅ 合格: ラッキーパンチ抜きでも勝てています。")
🗣️ アピールポイント (Why this matters)
このプロジェクトは、単に「競馬を当てる」ことだけが目的ではありません。 **「不確実性の高いデータから、論理的なアプローチでエッジ（優位性）を見つけ出し、システムとして実装・運用する力」**を実証するものです。 データエンジニアリング、機械学習、そしてWeb技術を統合した実践的な開発経験を示しています。
